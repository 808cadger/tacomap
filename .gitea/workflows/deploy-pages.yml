name: Deploy Pages

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Deploy to Codeberg Pages
        uses: codeberg.org/Codeberg-CI/pages@v1
        with:
          pages_branch: pages
          static_path: .
